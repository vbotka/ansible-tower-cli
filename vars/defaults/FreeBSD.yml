---
# FreeBSD vars for tower-cli

tcli_pip_install: true

tcli_packages:
  - name: 'ansible-tower-cli'

tcli_pip_executable: "{{ pip_executable_freebsd|default('/usr/local/bin/pip-3.6') }}"

tcli_links: []

# EOF
...
